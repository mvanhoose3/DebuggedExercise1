Question: You have been tasked with reviewing and updating this program. What steps would you take to identify any issues in the code?
* First, I'd double-check all my variable names—making sure they're spelled right and used the same way everywhere. That's always my starting point.
* Next, I'd look at the input and output. I want to make sure every time the code asks for something, there's actually a corresponding input. For example, if 'firstTest' is missing before the loop, that's a red flag.
* I'd also pay close attention to how the loop works—is the condition really what I want, and does the variable inside the loop actually get updated like it's supposed to?
* Sometimes, I'll just run through the logic myself with sample values (like plugging in 100 for the first test) to see if the loop acts the way I expect. That's basically what the document does, too.
* When it comes to math, I make sure all the formulas are written correctly—especially with parentheses, so the calculations don't get messed up.
* I also check any if/else statements to make sure the pass/fail logic is actually doing what I want.
* And of course, I look out for silly typos—like writing 'ouput' instead of 'output.'





Question: What actions could the developer have taken to minimize the number of errors?
Here’s what the developer could’ve done:
* Kept variable names consistent and double-checked spelling as they went.
* Added plenty of comments to explain the logic and avoid confusion later.
* Tested the program bit by bit, making sure each part works before moving on.
* Stuck to a neat format, with clear indentation and paired up each input and output.
* Double-checked the math to make sure parentheses are in the right place, so the calculations don’t go sideways.
* Walked through the code step by step (just like in the document) to spot anything missing or any logic gaps early on.





Debbugging Exercise:

// This pseudocode is intended to determine whether students have
// passed or failed a course; student needs to average 60 or
// more on two tests. 


start
   Declarations
      num firstTest
      num secondTest
      num average
      num PASSING = 60

   output "Enter first score or 0 to quit "
   input firstTest

   while firstTest <> 0
      output "Enter second score "
      input secondTest

      average = (firstTest + secondTest) / 2

      output "Average is ", average

      if average >= PASSING then
         output "Pass"
      else
         output "Fail"
      endif

      output "Enter first score or 0 to quit "
      input firstTest
   endwhile
stop

